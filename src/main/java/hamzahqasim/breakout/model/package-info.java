/**
 * Plain data/behavior classes for the objects on the board (ball, paddle,
 * bricks). No Swing, no game rules - just shapes that know how to move and
 * report their bounds, which is what keeps them trivial to unit test.
 */
package hamzahqasim.breakout.model;
